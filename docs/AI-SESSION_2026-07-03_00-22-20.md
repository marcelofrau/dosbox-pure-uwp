# AI Session Checkpoint — 2026-07-03 00:22:20

> Marco temporal. Estado completo do projeto antes de pausar.
> Para retomar: ler este arquivo + `ROADMAP.md` + `AGENTS.md` + `CLAUDE.md`.

---

## Sumário Executivo

Projeto: portar **dosbox-pure** (libretro) para **UWP** (Windows/Xbox).
Build: 0 erros Release|x64. Vídeo funciona. Input quebrado. Áudio não conectado.

---

## Git Log

```
61064f2 docs: AGENTS.md + ARCHITECTURE/REFERENCES updates
5e8da87 feat: libretro frontend + D2D render + file picker
d9bb67f feat: UWP port — build succeeded, patches local, DBPS_* stubs
31d9ea3 Phase 1: Build succeeds — linker errors resolved, all UWP patches integrated
7d1a331 Phase 0.5: Core build integration scaffold + docs
8e7f071 Initial commit: dosbox-pure-unleashed-uwp UWP port
```

Submodule: `extern/dosbox-pure` at `e166344` (contém patches UWP).

---

## Estrutura de Arquivos

### Frontend (nossa parte)

| Caminho                                           | Função                                                 |
| ------------------------------------------------- | ------------------------------------------------------ |
| `dosbox-uwp/App.cpp/.h`                           | Entry point, F1 picker, async file read → LoadRom      |
| `dosbox-uwp/dosbox_uwpMain.cpp/.h`                | Update/Render loop, input routing, core lifecycle      |
| `dosbox-uwp/Content/RetroCore.cpp/.h`             | Libretro bridge: init/load/run/callbacks/retro_env/VFS |
| `dosbox-uwp/Content/RetroScreenRenderer.cpp/.h`   | D2D bitmap + letterbox                                 |
| `dosbox-uwp/Content/SdlInput.cpp/.h`              | SDL gamepad + UWP Gamepad API fallback                 |
| `dosbox-uwp/Content/Sample3DSceneRenderer.cpp/.h` | Cubo 3D fallback (sem ROM)                             |
| `dosbox-uwp/Content/SampleFpsTextRenderer.cpp/.h` | Debug overlay                                          |
| `dosbox-uwp/dosbox_pure_sta.cpp`                  | DBPS_* stubs (12 no-ops)                               |
| `dosbox-uwp/dosbox-uwp.vcxproj`                   | Projeto MSBuild                                        |
| `dosbox-uwp/uwp-dep.props`                        | Propriedades SDL (importado via $(SolutionDir))        |

### Local patches (cópia editada do core)

| Caminho                                                 | O que muda                      |
| ------------------------------------------------------- | ------------------------------- |
| `dosbox-uwp/local/dosbox-pure/dosbox_pure_libretro.cpp` | UWP tweaks (SET_HW_RENDER, etc) |
| `dosbox-uwp/local/dosbox-pure/src/misc/cross.cpp`       | getcwd/chdir path compat        |
| `dosbox-uwp/local/dosbox-pure/src/gui/midi.cpp`         | MIDI disabled (no UWP API)      |
| `dosbox-uwp/local/dosbox-pure/src/dbp_serialize.cpp`    | Serialization compat            |
| `dosbox-uwp/local/dosbox-pure/src/dos/drive_cache.cpp`  | std::filesystem compat          |

### Dependências externas

| Caminho                                                 | Origem                           |
| ------------------------------------------------------- | -------------------------------- |
| `extern/dosbox-pure/`                                   | Submodule schellingb/dosbox-pure |
| `extern/libretro-common/vfs/vfs_implementation_uwp.cpp` | RetroArch                        |
| `extern/libretro-common/include/vfs/vfs.h`              | RetroArch                        |
| `extern/libretro-common/include/retro_environment.h`    | RetroArch                        |

### Docs

| Caminho                | Conteúdo                                              |
| ---------------------- | ----------------------------------------------------- |
| `docs/ROADMAP.md`      | Fases detalhadas com status por subitem               |
| `docs/ARCHITECTURE.md` | Diagrama + pipeline + decisões técnicas               |
| `docs/REFERENCES.md`   | Pesquisa upstream, erros corrigidos, D2D/UWP findings |
| `AGENTS.md`            | Regras críticas para AI agents                        |
| `CLAUDE.md`            | Memory file (lessons learned, bugs)                   |
| `docs/AI-SESSION_*.md` | *Este arquivo*                                        |

---

## Status por Fase

### Fase 0 — Scaffold ✅

Template DX11 UWP, SDL2 (gamecontroller + haptic + audio), StepTimer, debug overlay, build scripts, cert.

### Fase 1 — Core Build ✅

15 source files do core compilados. 5 patches locais. VFS UWP do RetroArch. Build 0 erros.

### Fase 2 — Frontend Stub 🏗️

- ✅ Lifecycle: init, load, run, unload, deinit
- ✅ retro_env: GET_VFS_INTERFACE, GET_SYSTEM_DIR, GET_SAVE_DIR, SET_PIXEL_FORMAT, SET_HW_RENDER (return 0), SET_MESSAGE_EXT, SET_KEYBOARD_CALLBACK, GET_THROTTLE_STATE, SHUTDOWN
- ❌ GET_VARIABLE (cmd 15) — retorna 0, launcher não aparece
- ❌ SET_VARIABLES (cmd 16) — não implementado, core não registra variáveis
- ❌ GET_CURRENT_SOFTWARE_FRAMEBUFFER (cmd 55)
- ❌ Demais: GET_LIBRETRO_PATH, SET_SUPPORT_ACHIEVEMENTS, SET_SUPPORT_NO_GAME, GET_AUDIO_VIDEO_ENABLE
- ✅ Smoke test: boot, F1 picker, load, video renderiza, logging

### Fase 3 — Video ✅ (1 pending)

- retro_video callback com pitch==0 guard
- D2D bitmap: XRGB8888 + ALPHA_IGNORE + GetDpi() + CopyFromMemory
- BeginDraw/DrawBitmap/EndDraw com letterbox
- D2DERR_RECREATE_TARGET handling
- Pendente: medir FPS baseline

### Fase 4 — Input 🏗️

- ✅ retro_input_state: KEYBOARD, JOYPAD (parcial), MOUSE, POINTER
- ✅ SDL gamepad connect/disconnect events
- ✅ UWP Gamepad API fallback (Xbox)
- ❌ **BUG CRÍTICO**: OnKeyEvent usa VirtualKey codes (0x0D, 0x1B, 0x26...) em vez de RETROK_*
  - A-Z e 0-9 funcionam por acidente (VK == ASCII)
  - Enter, Escape, Backspace, Tab, Setas, Shift, Ctrl, Alt — mapeados como VK, core ignora
- ❌ RETRO_DEVICE_ANALOG (thumbsticks) — stub retorna 0
- ❌ SDL gamepad → libretro JOYPAD mapping (BUTTON_A, D-pad, etc)
- ❌ F1-F12 mapping (F1 conflita com picker)
- ❌ Numpad, punctuation

### Fase 5 — Audio 🔲

- ✅ retro_audio_sample_batch armazena em s_audioBuffer com mutex
- ✅ GrabAudio() retorna ponteiro + frame count
- ❌ Update() nunca chama GrabAudio()
- ❌ Buffer não enviado para SDL_QueueAudio
- ⚠️ SDL audio device já existe (44100Hz, S16SYS, mono) — beep test funciona
- ⚠️ retro_audio REPLACE buffer em vez de append — precisaria ring buffer

### Fase 6 — Config/Saves 🔲

- Nada implementado
- DBPS_* stubs (12 funções) são no-op

### Fase 7 — Deploy 🏗️

- ✅ F1 FileOpenPicker com 11 extensões
- ✅ Async read via FileIO::ReadBufferAsync + .then()
- ❌ MSIX packaging, Xbox deploy, performance profile, splash screen

---

## Known Bugs

| #   | Bug                                    | Local                             | Severidade                |
| --- | -------------------------------------- | --------------------------------- | ------------------------- |
| 1   | OnKeyEvent usa VK codes não RETROK_    | `dosbox_uwpMain.cpp:216-244`      | 🔴 Bloqueia input teclado |
| 2   | GET_VARIABLE retorna 0                 | `RetroCore.cpp:308-312` (default) | 🔴 Bloqueia launcher      |
| 3   | GrabAudio nunca chamado                | `dosbox_uwpMain.cpp:99-147`       | 🟡 Sem áudio              |
| 4   | retro_audio replace em vez de append   | `RetroCore.cpp:342-351`           | 🟡 Perde dados            |
| 5   | MOUSE/POINTER sem evento real de mouse | `App.cpp` (sem handler)           | 🟡 Stub não conectado     |
| 6   | ~1500 C4244 warnings                   | Todo core                         | 🟢 Cosmético              |

---

## Para Retomar Amanhã

### Prioridade 1 — Input (Fase 4.2)

Corrigir `dosbox_uwpMain::OnKeyEvent()`:

- Mapear `Windows::System::VirtualKey` para `RETROK_*` (libretro.h define RETROK_a..RETROK_z, RETROK_0..RETROK_9, RETROK_F1..F12, RETROK_RETURN, RETROK_ESCAPE, etc.)
- Usar `#include "libretro.h"` e referência `RETROK_*`
- F1: conflita com picker — decidir se F1 abre menu OU pasa para core

### Prioridade 2 — Config Variables (Fase 2.5)

Implementar `retro_env GET_VARIABLE`:

- Fonte verdade: `extern/dosbox-pure/core_options.h` — ~70 variáveis definidas em `option_defs[]`
- Implementar `SET_VARIABLES` (cmd 16) primeiro (registra variáveis no core)
- Depois `GET_VARIABLE` (cmd 15) para cada key, retornando default values
- Variável chave para launcher: `dosbox_pure_menu_time` (default "99")

### Prioridade 3 — Audio (Fase 5.2)

- Em `Update()`, após `retro_run()`, chamar `m_retroCore->GrabAudio()`
- Enviar samples para `SDL_QueueAudio(m_sdlInput->m_audioDevice, ...)`
- Mutex: usar `s_audioMutex` (já existe)
- Considerar ring buffer em vez de replace

---

## Comandos Úteis

```powershell
# Build
MSBuild.exe "dosbox-pure-unleashed-uwp.sln" /p:Configuration=Release /p:Platform=x64 /nowarn:MSB4011

# Ver submodule commit
git submodule status

# Ver diff dos patches locais vs submodule
git diff --no-index extern/dosbox-pure/dosbox_pure_libretro.cpp dosbox-uwp/local/dosbox-pure/dosbox_pure_libretro.cpp
```



 opencode
                                   ▄
  █▀▀█ █▀▀█ █▀▀█ █▀▀▄ █▀▀▀ █▀▀█ █▀▀█ █▀▀█
  █  █ █  █ █▀▀▀ █  █ █    █  █ █  █ █▀▀▀
  ▀▀▀▀ █▀▀▀ ▀▀▀▀ ▀▀▀▀ ▀▀▀▀ ▀▀▀▀ ▀▀▀▀ ▀▀▀▀

  Session   Uso de SDL para gamepad e beep
  Continue  opencode -s ses_0dd267f95ffeqToNqnSSDmxYSM
