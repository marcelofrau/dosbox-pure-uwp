# Video enhancements — Tasks

1. Add `ScaleMode` enum to `RetroScreenRenderer.h`
2. Implement pixel-perfect rendering mode
3. Implement integer scale rendering mode
4. Implement aspect-ratio rendering mode
5. Keep existing stretch mode as default
6. Add keyboard shortcut to cycle scale modes (e.g. F2)
7. Add FPS overlay toggle
8. Connect core video options to renderer behavior
9. Measure and log FPS baseline
10. Build and test all scale modes with various game resolutions
